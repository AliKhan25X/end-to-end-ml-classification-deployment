# Model Evaluation Report

Run the training pipeline to generate the latest metrics:

```bash
python -m src.train
```

This file is a placeholder so the repository has a clear report location before the first training run.
